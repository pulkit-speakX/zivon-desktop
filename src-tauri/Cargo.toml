[package]
name = "zivon-desktop"
version = "0.1.0"
description = "Envoy desktop companion"
authors = ["Zivon"]
edition = "2021"
rust-version = "1.77"

# The binary that `tauri` runs.
[[bin]]
name = "zivon-desktop"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon", "image-png"] }
tauri-plugin-global-shortcut = "2"
tauri-plugin-notification = "2"
tauri-plugin-single-instance = "2"
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
keyring = { version = "3", features = ["apple-native"] }
tiny_http = "0.12"
uuid = { version = "1", features = ["v4"] }
urlencoding = "2"
ureq = { version = "2", features = ["json"] }
dirs = "5"

[features]
# By default Tauri runs in production mode; `tauri dev` flips this.
default = []
